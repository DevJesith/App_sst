import 'package:flutter/material.dart';

// Clase que centraliza los colores utilizados en la aplicación.
// Esto permite mantener consistencia visual y facilita cambios globales de estilo.

class AppColors {
  // Color principal de la app (usualmente para barras, botones destacados, etc.)
  static const Color primary = Color(0xFF1976D2);

  // Color secundario (puede usarse para hover, acentos, o elementos complementarios)
  static const Color secondary = Color(0xFF42A5F5);

  // Color de fondo general (pantallas, contenedores principales)
  static const Color background = Colors.white;
}
